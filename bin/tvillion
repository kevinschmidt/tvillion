#!/usr/bin/env ruby

require_relative '../lib/tvillion'

if __FILE__ == $0
  d = TVillion::Downloader.new()
  show = d.generate_show(ARGV[0])
  puts show
  t = d.get_search_results(show.generate_search_string())
  puts t
    
  trans = TVillion::Transmission::Client.new('media.lan', '9091')
  #trans.add_torrent(t)
end
